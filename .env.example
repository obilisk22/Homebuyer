# Optional: only used to prefer Google Geocoding for Map pins.
# Leave blank — Map/Street View work free via OpenStreetMap Nominatim + free Google iframes.
# Street View does NOT use Google Cloud billing.
GOOGLE_MAPS_API_KEY=

# App
HOMEBUY_DB_PATH=data/homebuy.db
HOMEBUY_HOST=127.0.0.1
HOMEBUY_PORT=8080
